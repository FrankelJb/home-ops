---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: stump
  namespace: selfhosted
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      PUID: 1000
      PGID: 1000
      STUMP_PORT: 10801
    image:
      repository: aaronleopold/stump
      tag: nightly@sha256:55d48e9eebb36ec5443b6203dae00350122726bef5a1a48821a9d3cda84c6a8b
      pullPolicy: IfNotPresent
    persistence:
      data:
        enabled: true
        path: /mnt/raid/media/books
        server: ${NFS_HOST}
        type: nfs
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host stump.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    service:
      main:
        ports:
          http:
            port: 10801
