---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: stump
  namespace: selfhosted
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      PUID: 1000
      PGID: 1000
      STUMP_PORT: 10801
    image:
      repository: aaronleopold/stump
      tag: nightly@sha256:c623aea7c5924c4261a8e7859d4611c3d45498a66447e9090d19ab35ccc29c88
      pullPolicy: IfNotPresent
    persistence:
      data:
        enabled: true
        path: /mnt/raid/media/books
        server: ${NFS_HOST}
        type: nfs
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host stump.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    service:
      main:
        ports:
          http:
            port: 10801
