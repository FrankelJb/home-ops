---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: matrix-synapse
  namespace: selfhosted
spec:
  interval: 15m
  chart:
    spec:
      chart: matrix-synapse
      version: 3.5.0
      sourceRef:
        kind: HelmRepository
        name: ananace
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    config:
      enableRegistration: false
      registrationSharedSecret: ${SECRET_MATRIX_REGISTRATION_TOKEN}
      trustedKeyServers:
        - server_name: ""
    extraConfig:
      registration_requires_token: true
      suppress_key_server_warning: true
    externalRedis:
      host: ${REDIS_HOST}
      password: ${REDIS_PASSWORD}
    externalPostgresql:
      host: ${POSTGRESQL_HOST}
      port: 5432
      username: synapse
      database: synapse
      existingSecret: matrix-synapse-extra
      existingSecretPasswordKey: postgresql-password
    image:
      repository: ghcr.io/matrix-org/synapse
      tag: v1.88.0@sha256:3c81deceeee138911c7410f8faf79e942bdde79f25146a314c7002d003d634c1
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host "matrix.${ROOT_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    persistence:
      enabled: true
      existingClaim: matrix-synapse-pvc
    postgresql:
      enabled: false
    redis:
      enabled: false
    serverName: *host
    signingkey:
      job:
        enabled: false
      existingSecret: matrix-synapse-extra
      existingSecretKey: signing.key
