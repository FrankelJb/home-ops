---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: uptime-kuma
  namespace: utilities
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      TZ: "${TIMEZONE}"
    # Waiting on rootless image first:
    # https://github.com/louislam/uptime-kuma/issues/2113
    # https://github.com/louislam/uptime-kuma/pull/2086
    # podSecurityContext:
    #   runAsUser: "${SECURITY_CONTEXT_RUN_AS_USER}"
    #   runAsGroup: "${SECURITY_CONTEXT_RUN_AS_GROUP}"
    image:
      repository: docker.io/louislam/uptime-kuma
      tag: 1.23.1@sha256:68b21d3f057ac2bfa12192c2606741da8ed713238a0406cdaecffd465de18637
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host "uptime.${ROOT_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    persistence:
      data:
        enabled: true
        mountPath: /app/data
        existingClaim: uptime-kuma-pvc
    service:
      main:
        ports:
          http:
            port: 3001
