---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: renovate
  namespace: utilities
spec:
  interval: 15m
  chart:
    spec:
      chart: renovate
      version: 35.159.5
      sourceRef:
        kind: HelmRepository
        name: renovate
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      LOG_LEVEL: debug
    existingSecret: renovate-env
    image:
      repository: ghcr.io/renovatebot/renovate
      tag: 36.1.7@sha256:56df0429a7da1faa32963bdbaf372e2b3febe37c45f6d83473a66584f2a3ed5e
      pullPolicy: IfNotPresent
    cronjob:
      schedule: "0 */6 * * *"
    renovate:
      config: |
        {
            "$schema": "https://docs.renovatebot.com/renovate-schema.json",
            "extends": [
                "config:base",
                ":semanticCommits",
                ":dependencyDashboard",
                ":automergeDigest",
                ":automergeBranch",
            ],
            "platform": "gitea",
            "autodiscover": false,
            "dependencyDashboard": true,
            "dependencyDashboardTitle": "Renovate Dashboard 🤖",
            "dryRun": false,
            "printConfig": true,
            "kubernetes": {
                "fileMatch": [
                    ".yaml$"
                ]
            },
            "major": {
                "dependencyDashboardApproval": true
            },
            repositories: ["gitea_admin/home-ops"]
        }
