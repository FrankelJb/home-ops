---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: pgadmin
  namespace: utilities
spec:
  interval: 15m
  chart:
    spec:
      chart: pgadmin4
      version: 1.15.2
      sourceRef:
        kind: HelmRepository
        name: runix
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      email: admin@${ROOT_DOMAIN}
    existingSecret: pgadmin-secrets
    image:
      registry: docker.io
      repository: dpage/pgadmin4
      tag: 7.3@sha256:c60a0271da93554f05aa0d358e0fa899a0cb5662501b68c86df8696c579df8fc
    ingress:
      enabled: true
      ingressClassName: nginx
      hosts:
        - host: &host "pgadmin.${ROOT_DOMAIN}"
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - *host
    persistentVolume:
      enabled: true
      existingClaim: pgadmin-pvc
