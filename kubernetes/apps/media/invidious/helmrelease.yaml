---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: invidious
  namespace: downloads
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    enableServiceLinks: false
    envFrom:
      - secretRef:
          name: invidious
    image:
      repository: quay.io/invidious/invidious
      tag: latest@sha256:a3f19940a553fa395ce4c0c6c891a400cccd8e3890c0d8ce0e619e9e9ea5cd75
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host invidious.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    service:
      main:
        ports:
          http:
            port: 3000
