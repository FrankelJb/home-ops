---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: collabora
  namespace: cloud
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      extra_params: --o:ssl.termination=true --o:ssl.enable=false
    image:
      repository: collabora/code
      tag: 23.05.4.2.1@sha256:d987ceafa0ea56fdbd55e6dd7b93ec8c01dfd3b74f78d8c75ee52eedb61ca949
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host collabora.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    service:
      main:
        ports:
          http:
            port: 9980
