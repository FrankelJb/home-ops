---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: collabora
  namespace: cloud
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 15m
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    env:
      extra_params: --o:ssl.termination=true --o:ssl.enable=false
    image:
      repository: collabora/code
      tag: 23.05.1.3.1@sha256:5be1c9fcd08a091d194a1ade51a39a99592ec4037942927a2271380b105dba80
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host collabora.${ROOT_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host
    service:
      main:
        ports:
          http:
            port: 9980
